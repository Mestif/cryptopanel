# 📤 Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub.com](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name:** `cryptopanel` (или другое имя)
   - **Description:** `macOS приложение для отслеживания криптовалют в меню баре`
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd /Volumes/Kingston/cryptopanel

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/cryptopanel.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Если используете SSH:**
```bash
git remote add origin git@github.com:YOUR_USERNAME/cryptopanel.git
git branch -M main
git push -u origin main
```

## Шаг 3: Обновите README.md

После загрузки на GitHub, откройте файл `README.md` и замените:
- `YOUR_USERNAME` на ваш реальный GitHub username (в ссылках на Releases и Issues)

Или выполните команду:
```bash
# Замените YOUR_USERNAME на ваш GitHub username
sed -i '' 's/YOUR_USERNAME/ваш_username/g' README.md
```

Затем закоммитьте изменения:
```bash
git add README.md
git commit -m "Update README with GitHub username"
git push
```

## Шаг 4: Создайте первый Release (опционально)

1. Перейдите в раздел **Releases** на GitHub
2. Нажмите **"Create a new release"**
3. Заполните:
   - **Tag version:** `v1.8` (или другая версия)
   - **Release title:** `CryptoPanel v1.8`
   - **Description:** Скопируйте информацию из `UPDATE_v1.8.md`
4. Прикрепите файлы:
   - Если есть `.dmg` файл, загрузите его
   - Или `.zip` архив с приложением
5. Нажмите **"Publish release"**

## Шаг 5: Настройте GitHub Pages (опционально)

Если хотите создать сайт для проекта:
1. Перейдите в **Settings** → **Pages**
2. Выберите источник: **main branch** → **/docs** или **/root**
3. Сохраните

---

## ✅ Готово!

Теперь ваш проект доступен на GitHub:
- **Репозиторий:** `https://github.com/YOUR_USERNAME/cryptopanel`
- **Скачать:** `https://github.com/YOUR_USERNAME/cryptopanel/releases`
- **Issues:** `https://github.com/YOUR_USERNAME/cryptopanel/issues`

---

## 🔄 Обновление проекта

Когда вы внесете изменения:

```bash
cd /Volumes/Kingston/cryptopanel
git add .
git commit -m "Описание изменений"
git push
```

---

## 📝 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть удаленные репозитории
git remote -v

# Изменить URL удаленного репозитория
git remote set-url origin https://github.com/YOUR_USERNAME/cryptopanel.git
```

